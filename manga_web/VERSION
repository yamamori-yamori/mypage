# マンガページエディタ — バージョン単一ソース（SSOT）
# 変更したら必ず: node scripts/sync-version.js
# 確認: node scripts/sync-version.js --check
# タグ: node scripts/sync-version.js --tag   （annotated tag v{APP}）
#
# APP  = アプリ仕様 / UI / README・MANUAL の「現行」表示（Ver.APP）
# JSON = プロジェクトファイル version（serializer / createProject）
APP=4.4
JSON=1.3
